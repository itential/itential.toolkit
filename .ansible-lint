---
# .ansible-lint

# Exclude these files from the linter
exclude_paths:
  - .github

# Rules that we feel should not be errors but warnings
warn_list:
  - yaml[line-length]
  - var-naming[no-role-prefix]

# Rules to skip entirely (e.g., for private or custom collections)
skip_list:
  - missing-import
