all:
    vars:
        service_name: Jst
        service_type: applications
        action: start
    children:
        iap:
            hosts:
                deviap:
                    ansible_host: http://localhost:3000
                    ansible_connection: local    
